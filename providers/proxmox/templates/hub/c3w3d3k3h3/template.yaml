# yaml-language-server: $schema=../../../../config/schemas/template.schema.json
# -----------------------------------------------------------------------------
# Deployment template — hub/c3w3d3k3h3 (topology tier 5)
# Name reads as the pool list: c×3 + w×3 + d(b)×3 + k(afka)×3 + h(ub)×3. Fully
# segregated: dedicated tainted pools for MySQL, Kafka AND the Mojaloop core
# services (central-ledger, quoting, ALS, ml-api-adapter, settlement, bulk).
# Each dedicated pool has its own pg-N groups, so the environment chooses
# shared or dedicated PHYSICAL hosts per pool via its pg -> host mapping.
# -----------------------------------------------------------------------------
version: 1
description: "Mojaloop hub: 3 CP + 3 workers + dedicated 3-node MySQL, Kafka and Mojaloop-core pools"
