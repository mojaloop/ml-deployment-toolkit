legacy/*
todo.md

# Terraform / OpenTofu
# .terraform.lock.hcl is deliberately COMMITTED (multi-platform provider pins);
# do not re-add it here.
.terraform/
*.tfstate
*.tfstate.*
tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Guard: adopter data lives OUTSIDE this clone (../environments, ../artifacts).
# These rules exist only to catch accidental re-creation of the old in-clone
# layout — the directories themselves must not exist here at all.
artifacts/
environments/

# Secrets & Recovery
recovery-kit/
.env

# Rendering (Jsonnet vendor dependencies, downloaded by jb install)
rendering/*/vendor/
rendering/*/jsonnetfile.lock.json

# IDEs
.vscode/
.idea/

# OS
.DS_Store

CLAUDE.md

_*

perf-result/*

tools/renovate