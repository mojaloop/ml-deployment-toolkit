legacy/*
todo.md

# Terraform / OpenTofu
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Artifacts (generated by Terraform)
artifacts/

# Secrets & Recovery
recovery-kit/
config/environments/*/.env

# Per-environment config (use config.yaml.sample as a template)
config/environments/*/config.yaml
config/environments/*/values/

# Rendering (Jsonnet vendor dependencies, downloaded by jb install)
rendering/*/vendor/
rendering/*/jsonnetfile.lock.json

# IDEs
.vscode/
.idea/

# OS
.DS_Store

CLAUDE.md

_*

.env

# --- Environment configs: everything ignored by default. ---
# Only the exact example files whitelisted below are ever tracked.
# Rule order matters (last match wins) — do not reorder or "simplify" this block.
config/environments/*
!config/environments/mlf-lab1-cc1/
!config/environments/mlf-lab1-sw1/
config/environments/mlf-lab1-cc1/*
config/environments/mlf-lab1-sw1/*
!config/environments/mlf-lab1-cc1/config.yaml.sample
!config/environments/mlf-lab1-cc1/.env.sample
!config/environments/mlf-lab1-sw1/config.yaml.sample
!config/environments/mlf-lab1-sw1/.env.sample