# yaml-language-server: $schema=../../../../schemas/template.schema.json
# -----------------------------------------------------------------------------
# Deployment template — env/tps-10 (HA Mojaloop Hub, ~10 TPS)
# Provider-independent capacity template: identity only — tuning moved to values/ and patches/ overlays.
# as values/ and patches/ overlays. No talos/ fragments on AWS.
# -----------------------------------------------------------------------------
version: 1
description: "HA Mojaloop switch: 3 control-plane + 3 general workers"
