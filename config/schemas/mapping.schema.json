{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Provider mapping",
  "description": "Schema for config/templates/mappings/*.yaml — translates capacity templates into provider-specific resources. Intentionally permissive: mapping content is provider-shaped and distribution-maintained.",
  "type": "object",
  "required": ["version"],
  "additionalProperties": true,
  "properties": {
    "version": {
      "description": "Mapping format version",
      "type": "integer",
      "minimum": 1
    }
  }
}
