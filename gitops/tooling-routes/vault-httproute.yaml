apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault
  namespace: vault
spec:
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hostnames:
    - "vault.int.${domain}"
  rules:
    - backendRefs:
        - name: vault
          port: 8200
