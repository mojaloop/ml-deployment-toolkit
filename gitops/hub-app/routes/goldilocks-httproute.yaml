# Hub-only: the tooling cluster has no IAM to gate this UI behind, so it
# exposes no route — operators port-forward there. Auth delegates to
# Oathkeeper against the goldilocksUi document (hub-iam-config).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: goldilocks
  namespace: goldilocks
spec:
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hostnames:
    - "goldilocks.int.${DOMAIN}"
  rules:
    - filters:
        # The scope is the decision endpoint's word, so a client's own copy is
        # removed at the edge and cannot reach anything behind it
        - type: RequestHeaderModifier
          requestHeaderModifier:
            remove:
              - X-Scope
        - type: ExternalAuth
          externalAuth:
            protocol: HTTP
            backendRef:
              name: oathkeeper-api
              namespace: ory
              port: 4456
            http:
              path: /decisions
              allowedHeaders:
                - cookie
                - accept
              allowedResponseHeaders:
                - x-scope
      backendRefs:
        - name: goldilocks-goldilocks-dashboard
          port: 80
