# Hub-only: the tooling cluster has no IAM to gate this UI behind, so it
# exposes no route — operators port-forward there. Auth delegates to
# Oathkeeper against the hubbleUi document (hub-iam-config).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hubble
  namespace: cilium
spec:
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hostnames:
    - "hubble.int.${DOMAIN}"
  rules:
    - filters:
        - type: ExternalAuth
          externalAuth:
            protocol: HTTP
            backendRef:
              name: oathkeeper-api
              namespace: ory
              port: 4456
            http:
              path: /decisions
              allowedHeaders:
                - cookie
                - accept
      backendRefs:
        - name: hubble-ui
          port: 80
