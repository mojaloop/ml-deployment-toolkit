# Distribution values for the reloader HelmRelease.
# Adopter overrides go in environments/<env>/values/reloader.yaml,
# which is merged after this file and therefore wins.
reloader:
  watchGlobally: true
  deployment:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        memory: 128Mi
