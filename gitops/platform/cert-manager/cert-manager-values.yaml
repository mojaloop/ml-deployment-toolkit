# Distribution values for the cert-manager HelmRelease.
# Adopter overrides go in environments/<env>/values/cert-manager.yaml,
# which is merged after this file and therefore wins.
installCRDs: true
config:
  enableGatewayAPI: true
prometheus:
  enabled: true
