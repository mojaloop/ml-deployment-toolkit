# Distribution values for the external-secrets HelmRelease.
# Adopter overrides go in environments/<env>/values/external-secrets.yaml,
# which is merged after this file and therefore wins.
webhook:
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 256Mi
