# Distribution values for the metrics-server HelmRelease.
# Adopter overrides go in environments/<env>/values/metrics-server.yaml,
# which is merged after this file and therefore wins.
args:
  - --kubelet-insecure-tls
resources:
  requests:
    cpu: 100m
    memory: 100Mi
  limits:
    memory: 200Mi
