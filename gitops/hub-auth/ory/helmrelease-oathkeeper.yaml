apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: oathkeeper
  namespace: flux-system
spec:
  interval: 30m
  chart:
    spec:
      chart: oathkeeper
      version: "0.62.1"
      sourceRef:
        kind: HelmRepository
        name: ory
      interval: 12h
  targetNamespace: ory
  install:
    createNamespace: false
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
      strategy: uninstall
  valuesFrom:
    # Distribution defaults. Adopter override is merged last and wins.
    - kind: ConfigMap
      name: oathkeeper-values
      valuesKey: values.yaml
    - kind: ConfigMap
      name: ory-oathkeeper-values-template
      valuesKey: values.yaml
      optional: true
    - kind: ConfigMap
      name: ory-oathkeeper-values-override
      valuesKey: values.yaml
      optional: true
    - kind: Secret
      name: ory-oathkeeper-values-override
      valuesKey: values.yaml
      optional: true
