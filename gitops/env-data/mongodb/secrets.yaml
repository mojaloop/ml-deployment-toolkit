apiVersion: v1
kind: Secret
metadata:
  name: bulk-mongodb-secrets
  namespace: data
type: Opaque
stringData:
  MONGODB_BACKUP_USER: backup
  MONGODB_BACKUP_PASSWORD: "${mongodb_root_password}"
  MONGODB_CLUSTER_ADMIN_USER: clusterAdmin
  MONGODB_CLUSTER_ADMIN_PASSWORD: "${mongodb_root_password}"
  MONGODB_CLUSTER_MONITOR_USER: clusterMonitor
  MONGODB_CLUSTER_MONITOR_PASSWORD: "${mongodb_root_password}"
  MONGODB_USER_ADMIN_USER: userAdmin
  MONGODB_USER_ADMIN_PASSWORD: "${mongodb_root_password}"
---
# Mongo app password. Lives in the data ns for PSMDB spec.users[].passwordSecretRef
# (operator only reads secrets in its own namespace) and is mirrored into the mojaloop ns
# for Mojaloop pods that reference bulk-mongodb-app-secrets directly.
apiVersion: v1
kind: Secret
metadata:
  name: bulk-mongodb-app-secrets
  namespace: data
type: Opaque
stringData:
  MONGODB_APP_PASSWORD: "${mongodb_app_password}"
---
apiVersion: v1
kind: Secret
metadata:
  name: bulk-mongodb-app-secrets
  namespace: mojaloop
type: Opaque
stringData:
  MONGODB_APP_PASSWORD: "${mongodb_app_password}"
