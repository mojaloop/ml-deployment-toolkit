apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: flux
  namespace: flux-system
spec:
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hostnames:
    - "flux.int.${DOMAIN}"
  rules:
    - backendRefs:
        - name: flux-operator
          port: 9080
