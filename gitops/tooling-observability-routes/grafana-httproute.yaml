apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: t7y-backend
spec:
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hostnames:
    - "grafana.int.${DOMAIN}"
  rules:
    - backendRefs:
        - name: grafana
          port: 80
