apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: observability
spec:
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hostnames:
    - "grafana.int.${domain}"
  rules:
    - backendRefs:
        - name: grafana
          port: 80
