# Distribution values for the iam HelmRelease (mojaloop-iam chart: Ory and
# the IAM services that compose the document of every backend a route keys
# into the deployment's gateway rules, Keto model and catalog).
# Adopter overrides go in environments/<env>/values/iam.yaml,
# which is merged after this file and therefore wins.
global:
  iamImage: docker.io/kirgene/ml-iam-services:iam-document-authz.7

  # One real thing under several services' names: a DFSP is `dfsps` where
  # the connection manager routes it and `participants` where the switch
  # and the reporting services return its rows.
  resourceNames:
    Participant:
      label: Participant
      description: A financial institution connected to the hub
      members:
        - { service: mcm, type: dfsps }
        - { service: reports, type: participants }
        - { service: trxreporting, type: participants }
        - { service: ledger, type: participants }
        - { service: settlement, type: participants }
      # The switch's registry is authoritative for who participates
      source:
        url: http://moja-centralledger-service.mojaloop.svc.cluster.local/participants
        id: /name
    Report:
      label: Report
      description: A report this deployment has defined
      members:
        - { service: reports, type: reports }

  # The first identity a fresh deployment logs in with: created while the
  # roles are applied and invited by email to set its password, the same
  # courier flow every other operator onboards through.
  iamAdmin:
    email: ${HUB_ADMIN_EMAIL}
    role: hub-admin

kratos:
  deployment:
    extraInitContainers: |
      - name: wait-for-db
        image: percona/percona-xtradb-cluster:8.0.44-35.1
        command: [sh, -c]
        args:
          - |
            # DSN format: mysql://user:pass@tcp(host:port)/db?params
            DB_USER=$(echo "$DSN" | sed 's|mysql://\([^:]*\):.*|\1|')
            DB_PASS=$(echo "$DSN" | sed 's|mysql://[^:]*:\([^@]*\)@.*|\1|')
            HOST=$(echo "$DSN" | sed 's|.*tcp(\([^:]*\):.*|\1|')
            PORT=$(echo "$DSN" | sed 's|.*tcp([^:]*:\([^)]*\)).*|\1|')
            DB=$(echo "$DSN" | sed 's|.*/\([^?]*\).*|\1|')
            echo "Waiting for MySQL connectivity to $DB..."
            until mysql -h "$HOST" -P "$PORT" -u "$DB_USER" -p"$DB_PASS" -e "SELECT 1" "$DB" 2>/dev/null; do
              echo "Waiting for $DB database..."
              sleep 5
            done
            echo "MySQL reachable. Verifying DDL readiness..."
            STABLE=0
            while [ $STABLE -lt 3 ]; do
              if mysql -h "$HOST" -P "$PORT" -u "$DB_USER" -p"$DB_PASS" "$DB" \
                -e "CREATE TABLE IF NOT EXISTS __init_check (id INT PRIMARY KEY) ENGINE=InnoDB; DROP TABLE IF EXISTS __init_check;" 2>/dev/null; then
                STABLE=$((STABLE + 1))
                echo "DDL check passed ($STABLE/3)"
              else
                STABLE=0
                echo "DDL not ready, retrying..."
              fi
              sleep 2
            done
            echo "Database ready!"
        env:
          - name: DSN
            valueFrom:
              secretKeyRef:
                name: kratos-secret
                key: dsn
  kratos:
    config:
      serve:
        public:
          base_url: https://kratos.int.${DOMAIN}/
          cors:
            enabled: true
            allow_credentials: true
            allowed_origins:
              - https://*.${DOMAIN}
      cookies:
        domain: ${DOMAIN}
        same_site: Lax
      session:
        cookie:
          domain: ${DOMAIN}
          same_site: Lax
      selfservice:
        default_browser_return_url: https://portal.int.${DOMAIN}/
        allowed_return_urls:
          - https://*.${DOMAIN}
        flows:
          login:
            ui_url: https://auth.int.${DOMAIN}/login
          error:
            ui_url: https://auth.int.${DOMAIN}/error
          recovery:
            ui_url: https://auth.int.${DOMAIN}/recovery
          settings:
            ui_url: https://auth.int.${DOMAIN}/settings
            # The invitation link chains recovery into settings and this
            # redirect completes it after the save
            after:
              default_browser_return_url: https://portal.int.${DOMAIN}/
      courier:
        smtp:
          from_name: Mojaloop Hub
          from_address: no-reply@${DOMAIN}

keto:
  deployment:
    extraInitContainers: |
      - name: wait-for-db
        image: percona/percona-xtradb-cluster:8.0.44-35.1
        command: [sh, -c]
        args:
          - |
            # DSN format: mysql://user:pass@tcp(host:port)/db?params
            DB_USER=$(echo "$DSN" | sed 's|mysql://\([^:]*\):.*|\1|')
            DB_PASS=$(echo "$DSN" | sed 's|mysql://[^:]*:\([^@]*\)@.*|\1|')
            HOST=$(echo "$DSN" | sed 's|.*tcp(\([^:]*\):.*|\1|')
            PORT=$(echo "$DSN" | sed 's|.*tcp([^:]*:\([^)]*\)).*|\1|')
            DB=$(echo "$DSN" | sed 's|.*/\([^?]*\).*|\1|')
            echo "Waiting for MySQL connectivity to $DB..."
            until mysql -h "$HOST" -P "$PORT" -u "$DB_USER" -p"$DB_PASS" -e "SELECT 1" "$DB" 2>/dev/null; do
              echo "Waiting for $DB database..."
              sleep 5
            done
            echo "MySQL reachable. Verifying DDL readiness..."
            STABLE=0
            while [ $STABLE -lt 3 ]; do
              if mysql -h "$HOST" -P "$PORT" -u "$DB_USER" -p"$DB_PASS" "$DB" \
                -e "CREATE TABLE IF NOT EXISTS __init_check (id INT PRIMARY KEY) ENGINE=InnoDB; DROP TABLE IF EXISTS __init_check;" 2>/dev/null; then
                STABLE=$((STABLE + 1))
                echo "DDL check passed ($STABLE/3)"
              else
                STABLE=0
                echo "DDL not ready, retrying..."
              fi
              sleep 2
            done
            echo "Database ready!"
        env:
          - name: DSN
            valueFrom:
              secretKeyRef:
                name: keto-secret
                key: dsn

hydra:
  deployment:
    extraInitContainers: |
      - name: wait-for-db
        image: percona/percona-xtradb-cluster:8.0.44-35.1
        command: [sh, -c]
        args:
          - |
            DB_USER=$(echo "$DSN" | sed 's|mysql://\([^:]*\):.*|\1|')
            DB_PASS=$(echo "$DSN" | sed 's|mysql://[^:]*:\([^@]*\)@.*|\1|')
            HOST=$(echo "$DSN" | sed 's|.*tcp(\([^:]*\):.*|\1|')
            PORT=$(echo "$DSN" | sed 's|.*tcp([^:]*:\([^)]*\)).*|\1|')
            DB=$(echo "$DSN" | sed 's|.*/\([^?]*\).*|\1|')
            echo "Waiting for MySQL connectivity to $DB..."
            until mysql -h "$HOST" -P "$PORT" -u "$DB_USER" -p"$DB_PASS" -e "SELECT 1" "$DB" 2>/dev/null; do
              echo "Waiting for $DB database..."
              sleep 5
            done
            echo "MySQL reachable. Verifying DDL readiness..."
            STABLE=0
            while [ $STABLE -lt 3 ]; do
              if mysql -h "$HOST" -P "$PORT" -u "$DB_USER" -p"$DB_PASS" "$DB" \
                -e "CREATE TABLE IF NOT EXISTS __init_check (id INT PRIMARY KEY) ENGINE=InnoDB; DROP TABLE IF EXISTS __init_check;" 2>/dev/null; then
                STABLE=$((STABLE + 1))
                echo "DDL check passed ($STABLE/3)"
              else
                STABLE=0
                echo "DDL not ready, retrying..."
              fi
              sleep 2
            done
            echo "Database ready!"
        env:
          - name: DSN
            valueFrom:
              secretKeyRef:
                name: hydra-secret
                key: dsn
  hydra:
    config:
      serve:
        public:
          cors:
            allowed_origins:
              - https://*.${DOMAIN}
      urls:
        self:
          issuer: https://hydra.ext.${DOMAIN}

# Browsers (accept text/html) get the Kratos login redirect, machines the
# json 401
oathkeeper:
  oathkeeper:
    config:
      authorizers:
        remote_json:
          config:
            remote: http://iam-ml-iam-services-decision/
      errors:
        fallback:
          - redirect
          - json
        handlers:
          redirect:
            enabled: true
            config:
              to: https://kratos.int.${DOMAIN}/self-service/login/browser
              return_to_query_param: return_to

# Kratos self-service UI at auth.<domain>, the browser talks to kratos.<domain>
kratos-ui:
  enabled: true
  kratosPublicUrl: http://kratos-public
  kratosBrowserUrl: https://kratos.int.${DOMAIN}
  projectName: Mojaloop Hub
  config:
    csrfCookieName: hub_csrf_token

# IdP surfaces: humans on the internal gateway, hydra (machine tokens) on
# the external one; these routes authenticate so they bypass Oathkeeper
gateway:
  enabled: true
  parentRefs:
    - name: gw-int
      namespace: platform-system
  hosts:
    kratos: kratos.int.${DOMAIN}
    auth: auth.int.${DOMAIN}
    # Renders only where the mail catcher is enabled (dev templates)
    mailpit: mailpit.int.${DOMAIN}
    hydra:
      host: hydra.ext.${DOMAIN}
      parentRefs:
        - name: gw-ext
          namespace: platform-system
  # Every namespace whose HTTPRoutes delegate auth to Oathkeeper through the
  # ExternalAuth filter
  oathkeeperConsumerNamespaces:
    - mcm
    - finance-portal
    - mojaloop
    - flux-system
    - goldilocks
    - cilium
