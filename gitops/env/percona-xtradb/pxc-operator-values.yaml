# Distribution values for the pxc-operator HelmRelease.
# Adopter overrides go in environments/<env>/values/pxc-operator.yaml,
# which is merged after this file and therefore wins.
watchNamespace: "data"
